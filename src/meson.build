glib_deps = [dependency('glib-2.0')]
gtk_deps = [dependency('gtk+-3.0')]

executable (meson.project_name(),

            'Main.vala',
            'Application.vala',
            'Window.vala',
            
            dependencies : [glib_deps, gtk_deps]
            )
